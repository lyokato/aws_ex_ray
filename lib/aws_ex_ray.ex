defmodule AwsExRay do

  def begin_segment(name) do
    %AwxExRay.Segment{}
  end

  def begin_sub_segment(segment, name) do
  end

  def close_segment(segment) do
  end

  def capture() do
  end

end
